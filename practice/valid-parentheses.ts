function isValid(s: string): boolean {
    const strArr = new Array(s)
    const isValid1 = true
    const isValid2 = true
    for (let i = 0; i < strArr.length; i++) {
        if (arr)
    }

    return false
};

console.log(isValid("{}{}"))